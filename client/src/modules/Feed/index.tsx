import styles from './index.module.scss';

import Header from '../../components/Header';

const Feed = () => {
    return (
        <div className={styles.root}>
            <Header />
        </div>
    );
}

export default Feed;